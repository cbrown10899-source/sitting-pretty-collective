# Frame Atelier work has moved

Everything that was staged here now lives in its own private repo:
**github.com/cbrown10899-source/frame-atelier** (branch `master`), which is
the single source of truth for the app, the SEO/content pages, and the
guided-capture feature. This folder existed only as temporary storage before
that repo was created (July 29, 2026); the full staging history is preserved
in this branch's earlier commits.
